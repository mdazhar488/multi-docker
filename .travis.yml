sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mdazhar488/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true mdazhar488/react-test npm test

after_success:
  - docker build -t mdazhar488/multi-client ./client
  - docker build -t mdazhar488/multi-nginx ./nginx
  - docker build -t mdazhar488/multi-server ./server
  - docker build -t mdazhar488/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push mdazhar488/multi-client
  - docker push mdazhar488/multi-nginx
  - docker push mdazhar488/multi-server
  - docker push mdazhar488/multi-worker
